# Créez le fichier test
cat > .github/workflows/test.yml << 'EOF'
name: Test Workflow

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Echo test
        run: echo "Hello World - GitHub Actions fonctionne !"
EOF